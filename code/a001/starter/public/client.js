(function() {
  const HOST = '';
  let page = document.body.id,
      methods = {
        index: initIndex,
        dashboard: initDashboard
      };

  methods[page]();


  // ================
  // Helper functions
  // ================
  function initIndex() {

  }

  function initDashboard() {

  }
})()
